Given a univariate time-series, the system automatically detects trend, seasonality, and anomalies, and outputs structured insights for downstream LLM explanation.

Constraints:

Single time series

Offline data

Batch processing

Python only